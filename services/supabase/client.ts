/**
 * Configuration centralisée du client Supabase pour le projet.
 * 
 * TODO: Une fois le package @supabase/ssr installé, remplacer ce boilerplate
 * par le vrai client createBrowserClient ou createServerClient.
 */

export const supabaseClientBoilerplate = {
  // auth: ...
  // from: ...
};
